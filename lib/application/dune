(library
  (name application)
  (libraries
    domain
    fmt
    logs)
  (inline_tests)
  (preprocess
    (pps
      ppx_deriving.show
      ppx_deriving.eq
      ppx_deriving.ord
      ppx_expect))
  (modules_without_implementation logger_sig))
